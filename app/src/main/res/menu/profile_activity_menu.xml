<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/fb_login"
        android:icon="@drawable/ic_notifications_active_white_24px"
        android:title="@string/user_profile"
        android:enabled="true"
        app:showAsAction="always" />
    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_settings"
        android:icon="@drawable/ic_settings_icon"
        android:title="@string/my_actionbar_settings"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_share"
        android:icon="@drawable/ic_notifications_active_white_24px"
        android:title="@string/my_actionbar_notifications"
        app:showAsAction="ifRoom" />

    <!-- This option to always be in the overflow -->
    <item
        android:id="@+id/action_rate"
        android:icon="@drawable/ic_menu_gallery"
        android:title="@string/my_actionbar_rate"
        app:showAsAction="never" />
</menu>